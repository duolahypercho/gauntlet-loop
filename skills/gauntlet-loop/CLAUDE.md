# Gauntlet Loop — Claude Code Overlay

Shared core: [AGENTS.md](AGENTS.md).

## Verbs for the prompt

| Slot | Value |
|---|---|
| `LOOP_VERB` | `/loop` |
| `CLOSING_TAIL` | ` and ultracode` |

## On `/gauntlet-loop`

Fill the three paragraphs in AGENTS.md. Run them. Enable `/loop` + ultracode if the host supports it. Fan out on the **product**. Separate harsh critic. Blind compare to the real reference. Keep going.

Do not invent helper scripts, capture suites, state files, or contracts. Pure prompt only.

Stop when the human stops you. Never ask "continue?"
