# Gauntlet Loop — Codex Overlay

Shared core: [AGENTS.md](AGENTS.md).

## Verbs for the prompt

| Slot | Value |
|---|---|
| `LOOP_VERB` | `/goal` |
| `CLOSING_TAIL` | `` (empty — no `ultracode` on Codex) |

Do **not** paste Claude-only tokens (`/loop`, `ultracode`) into a Codex run.

## On `$gauntlet-loop`

Fill the three paragraphs in AGENTS.md. Run them under `/goal` posture. Fan out on the **product**. Separate harsh critic. Blind compare to the real reference. Keep going.

Do not invent helper scripts, capture suites, state files, or contracts. Pure prompt only.

Stop when the human stops you. Never ask "continue?"
